backbone_name: vit_base_patch16_dinov3.lvd1689m
total_iters: 5000
eval_per_steps: 500
log_per_steps: 50

# Recall-first Dinomaly training parameters.
bottleneck_dropout: 0.1
grad_clip_norm: 1.0
hard_mining_final: 0.0
hard_mining_warmup_iters: 1000
easy_grad_factor: 0.1

# Preserve local anomaly evidence for low missed-detection operation.
score_top_k: 4
normal_score_percentile: 0.99
map_gaussian_sigma: 0.0
